---
import { t, changeLanguage } from "i18next";
import Layout from "~/layouts/PageLayout.astro";
import Features2 from "~/components/widgets/Features2.astro";
import ContactUs from "~/components/widgets/Contact.astro";

changeLanguage("de");

const metadata = {
    title: t("contact.title")
};
---

<Layout metadata={metadata}>
    <!-- HeroText Widget ******************* -->

    <!-- <HeroText tagline="Contact" title="Let's Connect!" /> -->

    <!-- Features2 Widget ************** -->

    <Features2
        title={t("contact.letsconnect") || "Error"}
        subtitle={t("contact.subtitle") || "Error"}
        items={[
            //   {
            //     title: 'Contact sales',
            //     description:
            //       'Chat with us for questions about purchases, customization options, licensing for commercial use, inquiries about specific template, etc.',
            //   },
            //   {
            //     title: 'Technical support',
            //     description:
            //       'Chat with us when facing issues like template installation, problems editing difficulties, compatibility issues with software or download errors, or other technical challenges related to using the templates.',
            //   },
            {
                title: t("contact.email.title") || "Error",
                description: "contact@think-ahead.tech",
                icon: "tabler:mail"
            },
            {
                title: t("contact.videocall.title") || "Error",
                description: t("contact.videocall.description") || "Error",
                icon: "tabler:video",
                callToAction: {
                    variant: "secondary",
                    target: "_blank",
                    text: t("contact.videocall.booknow") || "Error",
                    href: "https://outlook.office365.com/owa/calendar/ThinkAheadTechnologies@think-ahead.tech/bookings/",
                    icon: "tabler:calendar"
                }
            },
            {
                title: t("contact.post.title") || "Error",
                description: "Lautenschlagerstraße 16, 70173 Stuttgart",
                icon: "tabler:map-pin"
            }
        ]}
    />

    <ContactUs
        title={t("contact.form.title") || "Error"}
        subtitle={t("contact.form.subtitle") || "Error"}
        inputs={[
            {
                type: "text",
                name: "name",
                label: t("contact.form.name") || "Error"
            },
            {
                type: "email",
                required: true,
                name: "email",
                label: t("contact.form.email") || "Error"
            }
        ]}
        textarea={{
            label: t("contact.form.message") || "Error",
            required: true,
            placeholder: t("contact.form.message-placeholder") || "Error"
        }}
        disclaimer={{
            required: true,
            label: "contact.form.disclaimer"
        }}
        description={t("contact.form.description") || "Error"}
        button={t("contact.form.submit") || "Error"}
    />
</Layout>
